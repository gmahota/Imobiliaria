<h1>Listing fotosresposta</h1>

<table>
  <tr>
    <th>Descricao</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @fotosresposta.each do |fotosrespostum| %>
  <tr>
    <td><%= fotosrespostum.descricao %></td>
    <td><%= link_to 'Show', fotosrespostum %></td>
    <td><%= link_to 'Edit', edit_fotosresposta_path(fotosrespostum) %></td>
    <td><%= link_to 'Destroy', fotosresposta, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nova Resposta', new_fotosrespostum_path %>
