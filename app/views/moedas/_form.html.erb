<%= simple_form_for(@moeda) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :sigla %>
    <%= f.input :descricao %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
